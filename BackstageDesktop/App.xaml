<Application x:Class="Backstage.Desktop.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <SolidColorBrush x:Key="Bg" Color="#090D12"/>
        <SolidColorBrush x:Key="Panel" Color="#E00E1720"/>
        <SolidColorBrush x:Key="PanelSolid" Color="#101923"/>
        <SolidColorBrush x:Key="PanelHi" Color="#192531"/>
        <SolidColorBrush x:Key="Input" Color="#E40B1118"/>
        <SolidColorBrush x:Key="Gold" Color="#F6C34A"/>
        <SolidColorBrush x:Key="GoldHi" Color="#FFD66C"/>
        <SolidColorBrush x:Key="GoldDim" Color="#8E7134"/>
        <SolidColorBrush x:Key="Text" Color="#DDE4EA"/>
        <SolidColorBrush x:Key="Dim" Color="#8895A2"/>
        <SolidColorBrush x:Key="Green" Color="#4CCB82"/>
        <SolidColorBrush x:Key="Blue" Color="#8ABBDD"/>
        <LinearGradientBrush x:Key="GoldGradient" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#FFD263" Offset="0"/>
            <GradientStop Color="#E8A923" Offset="1"/>
        </LinearGradientBrush>

        <StreamGeometry x:Key="IconMusic">M9,19 L9,5 L20,3 L20,16 M9,8 L20,6 M9,19 C9,21 7,22 5,22 C3,22 2,21 2,19 C2,17 4,16 6,16 C8,16 9,17 9,19 M20,16 C20,18 18,19 16,19 C14,19 13,18 13,16 C13,14 15,13 17,13 C19,13 20,14 20,16</StreamGeometry>
        <StreamGeometry x:Key="IconHighway">M7,22 L10,2 M17,22 L14,2 M12,20 L12.4,16 M12.8,12 L13.2,8 M13.6,5 L14,2</StreamGeometry>
        <StreamGeometry x:Key="IconImage">M3,5 L21,5 L21,19 L3,19 Z M5,17 L10,11 L14,15 L17,12 L21,16 M8,9 A1.5,1.5 0 1 0 8.1,9</StreamGeometry>
        <StreamGeometry x:Key="IconPalette">M4,5 L20,5 M4,10 L20,10 M4,15 L20,15 M4,20 L20,20 M8,5 L8,20 M14,5 L14,20</StreamGeometry>
        <StreamGeometry x:Key="IconDownload">M12,3 L12,16 M7,11 L12,16 L17,11 M4,20 L20,20</StreamGeometry>
        <StreamGeometry x:Key="IconSettings">M4,6 L20,6 M4,12 L20,12 M4,18 L20,18 M9,6 A2,2 0 1 0 9.1,6 M16,12 A2,2 0 1 0 16.1,12 M11,18 A2,2 0 1 0 11.1,18</StreamGeometry>
        <StreamGeometry x:Key="IconSearch">M10,3 A7,7 0 1 0 10.1,3 M15,15 L21,21</StreamGeometry>
        <StreamGeometry x:Key="IconPlay">M8,5 L19,12 L8,19 Z</StreamGeometry>
        <StreamGeometry x:Key="IconList">M8,6 L21,6 M8,12 L21,12 M8,18 L21,18 M3,6 L4,6 M3,12 L4,12 M3,18 L4,18</StreamGeometry>

        <Style TargetType="Window">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="UseLayoutRounding" Value="True"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource Text}"/>
        </Style>

        <Style x:Key="GoldButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource GoldGradient}"/>
            <Setter Property="Foreground" Value="#101318"/>
            <Setter Property="BorderBrush" Value="#FFE199"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="17,10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="{StaticResource GoldHi}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Opacity" Value=".8"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Root" Property="Opacity" Value=".4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="GhostButton" TargetType="Button">
            <Setter Property="Background" Value="#B7101720"/>
            <Setter Property="Foreground" Value="#C5CED7"/>
            <Setter Property="BorderBrush" Value="#596571"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="15,9"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="7" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="BorderBrush" Value="{StaticResource Gold}"/>
                                <Setter Property="Foreground" Value="{StaticResource Gold}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Opacity" Value=".75"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="NavItem" TargetType="RadioButton">
            <Setter Property="Foreground" Value="#929EAA"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Height" Value="56"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="Root" Background="Transparent">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="68"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="3"/>
                                </Grid.ColumnDefinitions>
                                <Viewbox Width="23" Height="23" HorizontalAlignment="Center"
                                         VerticalAlignment="Center">
                                    <Path x:Name="Icon" Data="{TemplateBinding Tag}"
                                          Stroke="{TemplateBinding Foreground}"
                                          StrokeThickness="1.65" StrokeStartLineCap="Round"
                                          StrokeEndLineCap="Round" StrokeLineJoin="Round"
                                          Fill="Transparent"/>
                                </Viewbox>
                                <ContentPresenter Grid.Column="1" VerticalAlignment="Center"/>
                                <Border x:Name="Marker" Grid.Column="2" Background="{StaticResource Gold}"
                                        Visibility="Collapsed"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#171E2732"/>
                                <Setter Property="Foreground" Value="{StaticResource Text}"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#3A4A4227"/>
                                <Setter Property="Foreground" Value="{StaticResource Text}"/>
                                <Setter TargetName="Icon" Property="Stroke" Value="{StaticResource Gold}"/>
                                <Setter TargetName="Marker" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SearchBox" TargetType="TextBox">
            <Setter Property="Tag" Value="Buscar"/>
            <Setter Property="Background" Value="{StaticResource Input}"/>
            <Setter Property="Foreground" Value="{StaticResource Text}"/>
            <Setter Property="CaretBrush" Value="{StaticResource Gold}"/>
            <Setter Property="BorderBrush" Value="#47515D"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="44"/>
            <Setter Property="Padding" Value="42,0,14,0"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Root" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10">
                            <Grid>
                                <Viewbox Width="20" Height="20" Margin="15,0,0,0"
                                         HorizontalAlignment="Left" VerticalAlignment="Center"
                                         IsHitTestVisible="False">
                                    <Path Data="{StaticResource IconSearch}" Stroke="{StaticResource Gold}"
                                          StrokeThickness="1.8" StrokeStartLineCap="Round"
                                          StrokeEndLineCap="Round" Fill="Transparent"/>
                                </Viewbox>
                                <TextBlock x:Name="Hint" Text="{TemplateBinding Tag}"
                                           Foreground="#7F8995" FontSize="15" Margin="42,0,0,0"
                                           VerticalAlignment="Center" Visibility="Collapsed"
                                           IsHitTestVisible="False"/>
                                <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter TargetName="Hint" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Root" Property="BorderBrush" Value="{StaticResource Gold}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DarkCombo" TargetType="ComboBox">
            <Setter Property="Foreground" Value="{StaticResource Text}"/>
            <Setter Property="Background" Value="#D1111821"/>
            <Setter Property="BorderBrush" Value="#7D6738"/>
            <Setter Property="Height" Value="44"/>
            <Setter Property="Padding" Value="14,0,32,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                          Focusable="False" ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="{StaticResource PanelSolid}" BorderBrush="#7D6738"
                                                BorderThickness="1" CornerRadius="8">
                                            <Path Data="M 0 0 L 5 5 L 10 0 Z" Fill="{StaticResource Gold}"
                                                  HorizontalAlignment="Right" Margin="0,0,13,0"
                                                  VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter Content="{TemplateBinding SelectionBoxItem}"
                                              Margin="{TemplateBinding Padding}"
                                              VerticalAlignment="Center" IsHitTestVisible="False"/>
                            <Popup IsOpen="{TemplateBinding IsDropDownOpen}" Placement="Bottom"
                                   AllowsTransparency="True">
                                <Border Background="{StaticResource PanelSolid}" BorderBrush="{StaticResource GoldDim}"
                                        BorderThickness="1" CornerRadius="7" MinWidth="{TemplateBinding ActualWidth}">
                                    <ScrollViewer MaxHeight="300">
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="Foreground" Value="{StaticResource Text}"/>
                        <Setter Property="Padding" Value="12,8"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ComboBoxItem">
                                    <Border x:Name="Root" Background="Transparent" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsHighlighted" Value="True">
                                            <Setter TargetName="Root" Property="Background" Value="#263340"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ToggleCheck" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource Dim}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Application.Resources>
</Application>
